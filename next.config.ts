import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge sits on top of the Mapbox logo, which must stay visible.
  devIndicators: false,
};

export default nextConfig;
